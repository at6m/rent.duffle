import Link from 'next/link'
import { collections } from '@/lib/collections'
export function CollectionCard({collection}:{collection:typeof collections[number]}){return <Link href={`/collections/${collection.slug}`} className="collection panel group relative min-h-64 overflow-hidden p-6" style={{'--accent':collection.accent} as React.CSSProperties}><div className="absolute -right-8 -top-10 text-[170px] leading-none opacity-[.06] transition-transform duration-500 group-hover:rotate-12">✦</div><div className="relative flex h-full flex-col justify-between"><span className="font-display text-4xl opacity-40">0{collections.indexOf(collection)+1}</span><div><p className="eyebrow" style={{color:collection.accent}}>{collection.name}</p><p className="mt-2 max-w-[16rem] text-sm text-zinc-400">{collection.description}</p><p className="mt-5 text-xs text-zinc-600 group-hover:text-zinc-400">Explore collection →</p></div></div></Link>}
